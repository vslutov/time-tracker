language: node_js
node_js:
  - "stable"

jobs:
  include:
    - stage: build
      node_js: "stable"
      script:
        - npm run build
    - stage: deploy
      node_js: "stable"
      deploy:
        provider: pages
        local_dir: build
        skip_cleanup: true
        github_token: $GITHUB_TOKEN
        keep_history: true
        on:
          tags: true
