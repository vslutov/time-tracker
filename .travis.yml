language: node_js
node_js:
  - "10"

jobs:
  include:
    - stage: deploy
      node_js: "stable"
      deploy:
        provider: npm
        api_key: $NPM_API_KEY
        email: vs@lutov.net
        on:
          tags: true
