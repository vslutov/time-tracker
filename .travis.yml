language: node_js
node_js:
  - "10"

jobs:
  include:
    - stage: deploy
      node_js: "stable"
      deploy:
        provider: pages
        local_dir: build
        skip_cleanup: true
        github_token: $GITHUB_TOKEN
        keep_history: true
        on:
          tags: true
